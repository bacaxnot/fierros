# Shared Implementation Plan

Based on the spec defined in `packages/core/_specs_/contexts/training/shared.md`.

## Requirements

1. **MetricUnit**: Enum value object with unit types
2. **MetricValue**: Composite value object (value + unit)
3. **MetricValueRange**: Composite value object (min + max bounds)

## Analysis

All shared value objects already exist and match the spec:

- `src/contexts/training/shared/domain/metric-unit.ts` - MetricUnit with all specified values
- `src/contexts/training/shared/domain/metric-value.ts` - MetricValue with value + unit
- `src/contexts/training/shared/domain/metric-value-range.ts` - MetricValueRange with nullable min/max

## Folder Structure

```
packages/core/src/contexts/training/shared/domain/
├── metric-unit.ts                    # EXISTS - matches spec
├── metric-value.ts                   # EXISTS - matches spec
└── metric-value-range.ts             # EXISTS - matches spec
```

## Implementation Details

No changes required. Domain layer is complete and matches the spec.
